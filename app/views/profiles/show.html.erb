<div class="container">
  <%= render partial: 'profile_header', locals: {user: @user } %>
  <% @posts.each do |post| %>
    <%= render partial: "posts/post", locals: {post: post} %>
  <% end %>
</div>

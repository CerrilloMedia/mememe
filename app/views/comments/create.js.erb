$('#comments_<%= @post.id %>').append("<%=j render @comment, post: @post, comment: @comment %>");
$('#comment_content_<%= @post.id %>').val('')

// class methods provide the opporunity to utilize instance variables in JS files.
